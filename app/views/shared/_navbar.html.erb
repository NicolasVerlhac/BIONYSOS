<div class="navbar navbar-expand-sm navbar-light navbar-lewagon sticky-top">
  <%= link_to image_tag("Logo_Bio.png", alt: "logo"), '/', class: 'navbar-brand' %>
  <%= link_to "Notre Offre", pages_offer_path(@offer), class: 'navbar-links' %>
  <%= link_to "Nos Vignerons", vignerons_path, class: 'navbar-links' %>
  <%= link_to "Notre Engagement", pages_about_path(@about), class: 'navbar-links' %>
  <% if signed_in? %>
  <% else %>
    <div class="btn btn-flat-nav"> <%= link_to "je m'abonne", new_user_registration_path %> </div>
  <% end %>


  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>


  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <% if user_signed_in? %>

        <li class="nav-item dropdown">

          <div class="dropdown-toggle" id="navbarDropdown" data-toggle="dropdown">
            <%= current_user.first_name %>
          </div>

          <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
            <%= link_to "Mon profil", "#", class: "dropdown-item" %>
            <%= link_to "Se déconnecter", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
          </div>
        </li>
      <% else %>
        <li class="nav-item">
          <%= link_to "Login", new_user_session_path, class: "nav-link" %>
        </li>
      <% end %>
    </ul>
  </div>
</div>
